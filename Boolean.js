/* if (new Boolean(true)) {
  console.log('this boolean is true');
}

if (new Boolean(false)) {
  console.log('what about this boolean and this is also working');
}

const myFalse = new Boolean(false);
console.log(myFalse);

let name = Boolean(false);
console.log(name);

 */

let name = '';
console.log(name);
let boolean = Boolean(name);
console.log(boolean);
